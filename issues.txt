phyloai tree ml fasttree module overall re-review — responses

1. [High] --msa-dir / --matrix still use Click exists=True, missing-path errors bypass exit code 1.
   → ACCEPT. Removed exists=True from both --msa-dir and --matrix click options. Added manual existence validation in fasttree_command with explicit exit code 1. Updated test_tree_ml_fasttree_mutual_exclusivity to use real tmp_path dirs instead of /tmp.

2. [High] Single-file --matrix seq_type detection only parses FASTA, ignoring PHYLIP.
   → ACCEPT. Fixed single-matrix seq_type resolution in run_fasttree to branch by suffix: .phy/.phylip → "phylip-relaxed" format, else → "fasta".

3. [Medium] Test cli_msa_dir_nonexistent_exits_1 referenced from wrong file path.
   → ACCEPT. Moved test from tests/tree/test_ml.py to tests/cli/test_tree.py (where it belongs — it uses CliRunner). Updated verification command accordingly.

4. [Medium] Newick validation placed before returncode check, misclassifying failure reason.
   → ACCEPT. Reordered: returncode check block now comes before Phylo.read Newick validation. Non-zero exit returns tool-failure reason, not parse error.

5. [Medium] result.json.status = "success" even when all trees failed (exit code 2).
   → ACCEPT. _assemble_result now sets status = "error" (with non-null error message) when n_trees == 0 and (n_failed > 0 or n_skipped > 0). Matches parent spec schema contract: success | error.

6. [Low] PHYLIP wording inconsistent: "PHYLIP relaxed interleaved" vs "phylip-relaxed".
   → ACCEPT. Normalized to "phylip-relaxed (.phy, .phylip)" in tree-design.md §4 and fasttree-design.md §5.2. Kept descriptive text about FastTree's format support but used the standard identifier.


phyloai tree ml fasttree code review (round 2) — responses

1. [High] --resume cannot rerun failed FastTree tasks after a partially successful batch run.
   → ACCEPT. Only set checkpoint.status = "success" when len(failed_results) == 0. Otherwise keep "running" so plan_resume() can inspect individual task states on next --resume.

2. [High] Managed FastTree flags are not blocked in --tool-args, contrary to the final design doc.
   → REJECT. The design doc was updated per explicit user instruction: managed flags narrowed to {-nt, -expert, -help} only. All strategy flags are tool-args territory with override suppression.

3. [High] --dry-run still creates filesystem artifacts.
   → ACCEPT. In _assemble_result, skip output_dir.mkdir() and fasttree.log write when dry_run=True.

4. [Medium] --matrix mode does not fully validate supported formats.
   → ACCEPT. Validate extension against FASTTREE_COMPATIBLE_EXTENSIONS; raise on unsupported extension and empty parse.

5. [Medium] The CLI cannot actually disable gamma.
   → ACCEPT. Restored --gamma/--no-gamma flag pair (was --gamma is_flag=True one-way only).

6. [Medium] Error-to-exit-code mapping is too broad for "not found".
   → ACCEPT. Narrowed to "fasttree not found" — only FastTree binary not found maps to exit 3.

7. [Medium] Quiet batch dry-run with zero valid inputs can incorrectly exit 0.
   → ACCEPT. Removed `and not dry_run` guard on empty-input check in ml.py.

8. [Low] mean_n_taxa is always reported as 0.0.
   → ACCEPT. _run_one_fasttree now counts input sequences and records n_taxa in result.

9. [Low] One unit test depends on a real FastTree binary without guarding.
   → ACCEPT. Added @pytest.mark.skipif(not shutil.which("FastTree")) on test_run_one_fasttree_success.

Missing tests:
   → All 7 missing tests added: blocked -expert/-help, matrix unsupported/unparsable, dry-run no artifacts,
     resume all-succeeded skips all, n_taxa nonzero, --no-gamma CLI, quiet dry-run zero-input exits 1.
